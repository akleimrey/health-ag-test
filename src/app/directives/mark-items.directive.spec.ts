import { MarkItemsDirective } from './mark-items.directive';

describe('MarkItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkItemsDirective();
    expect(directive).toBeTruthy();
  });
});
